(* loader script for SML# *)

use "ml-yacc-lib.sml";
use "smlnj-lib.sml";

use "./Utility.sml";
use "./ErrorQueue.sml";

use "./FormatTemplate.sml";

use "./AST.sig";
use "./Ast.sml";
use "./ASTUTIL.sig";
use "./AstUtil.sml";

use "./ml.grm.sig";
use "./ml.grm.sml";

use "./TokenTable.sml";

(*
 * ToDo : We assume that mllex of MLton is used to generate lexer.
 *      If ml-lex of SML/NJ is used instead, use Constants_SMLNJ.sml, in order
 *      to correct wrong position information obtained from lexer generated by
 *      ml-lex of SML/NJ.
 *)
use "./Constants_MLton.sml";
(*
use "./Constants_SMLNJ.sml";
*)

use "./ml.lex.sml";

use "./MLPARSER.sig";
use "./MLParser.sml";

use "./FORMATTER_GENERATOR.sig";
use "./FormatterGenerator.sml";
use "./BasicFormattersEnv.sml";

use "./PPGMain.sml";

use "./Main.sml";

Main.main (CommandLine.name (), CommandLine.arguments ());