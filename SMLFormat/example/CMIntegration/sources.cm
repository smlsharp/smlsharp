Group
is
#if defined(NEW_CM)
        $/basis_compat.cm
#endif

        ../../smlformatlib.cm

#if (defined(structure PPGSourceTool))
        Tree.ppg:ppg
#else
        Tree.ppg.sml
#endif

        Main.sml
