Elaboration 2 
  expand record patterns
    fn {l=p,...} => e
        fn X => let p = #l1 X,...,pn = #ln X in e
  convert all the pattern matching other than fn
    val pat = exp 
         val (x,...,x) = (fn pat => (x,...,x)) exp
    case exp of match
	 (fn match) exp
    
 
